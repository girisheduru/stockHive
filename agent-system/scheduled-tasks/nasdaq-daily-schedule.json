{
  "task_id": "nasdaq-daily-top5-buys",
  "schedule": "0 17 * * 1-5",
  "timezone": "America/New_York",
  "agent": "stockhive-orchestrator",
  "entrypoint": "scripts/nasdaq-orchestrator-runtime.sh",
  "subagent_mode": "ephemeral",
  "prompt": "Run the StockHive agent system through the orchestrator and repo skills. First obtain a deterministic daily random sample of exactly 10 usable Nasdaq-100 tickers through the data-fetcher agent. Then run technical, fundamental, and sentiment analysis through their specialist agents in parallel. Aggregate the structured outputs, run the deterministic decision engine, and publish through the telegram-publisher agent using the primary runtime path.",
  "max_runtime_min": 20,
  "retry": { "attempts": 2, "backoff_sec": 120 },
  "notify_on_failure": true
}
